import alquiler from './alquiler/documents';
import alquiler from './alquiler/postAlquiler.js';
import messagesapp from "../data/messages.js";

class alquilerModel {

async createAlquiler(req) {

    console.log(`---> getModel::postAlquiler = ${id,isbn,data}`);

   
    if (typeof _alquiler == 'undefined')
        throw new Error(messagesapp.book_dosent_exist);

    alquiler.createAlquiler;
}
}
export default new alquilerModel()